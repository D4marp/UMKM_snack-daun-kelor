{
  "type": "module",
  "scripts": {
    "build": "postcss styles.css -o dist/styles.css && mkdir -p public && cp index.html public/ && cp -r dist public/ && cp -r images public/",
    "watch": "postcss styles.css -o dist/styles.css --watch"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.1.10",
    "postcss": "^8.5.5",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^4.1.10"
  }
}